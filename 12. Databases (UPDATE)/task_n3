SELECT 'http://' || users.id || '/', users.first_name, users.last_name, books.title
FROM users
LEFT JOIN purchase ON users.id = purchase.user_id
LEFT JOIN books ON purchase.book_id = books.id
ORDER BY users.id

